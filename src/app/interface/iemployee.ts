export interface IEmployee {
    employee_id: number;
    employee_role: 'leader' | 'employee';
    employee_name: string;
    employee_region: string;
    employee_email: string;
    employee_password: string;
}
